If you want to try one of the examples for your own project:

1) copy the contents of an example directory into ../Work/ (except
for the Makefile).
2) cd to Work/
3) Type 'make' to compile example.
4) Type do_roms.sh to run example. 

Available examples:

TANK - A analytical test case for the Non-hydrostatic kernel. 
       A sloshing solution in a tank.

